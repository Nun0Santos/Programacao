typedef struct cc{ //Dados de cada carta (infoDatas.dat)
    char nome[100];
    int nif;
    int id;
}cc;

typedef struct dono { //Dados de pessoas que adquiriram veiculos no último ano (infoDonos.dat)
    char nome[100];
    int nif;
    char mat[15];
}dono;